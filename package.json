{
  "name": "experiments",
  "scripts": {
    "clean": "rm -rf dist",
    "ts:compile": "tsc",
    "build": "npm run clean && npm run ts:compile"
  },
  "devDependencies": {
    "typescript": "^4.5.4"
  }
}
